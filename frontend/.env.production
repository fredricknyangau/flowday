# Production — update this URL once Railway assigns your backend domain
VITE_API_URL=https://flowday-production-5790.up.railway.app/api/v1
VITE_VAPID_PUBLIC_KEY=DUMMY_PUBLIC_KEY_SET_IN_PRODUCTION_ENV

# DO NOT commit the real value here — set VITE_API_KEY in Vercel's
# Project Settings → Environment Variables (Production environment only).
VITE_API_KEY=
